import { DashboardCard } from '../../../components/DashboardCard';
export default function BuurtPage() {
  return <main style={{ padding: 32, background: '#F5F3EE', minHeight: '100vh' }}><DashboardCard title="BUURT" subtitle="Elder-controlled neighbourhood visibility"><p>Family can see selected interest tags and accepted connection counts only. Other elder identities are never exposed.</p></DashboardCard></main>;
}
